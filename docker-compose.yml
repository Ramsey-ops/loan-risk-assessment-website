services:
  web:
    build: .
    image: loan-risk-assessment-website
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
    volumes:
      - ./database.json:/app/database.json
